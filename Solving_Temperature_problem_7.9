A body at a temperature of 50° F is placed outdoors where the temperature is 100° F. If after 5 minutes
the temperature of the body is 60° F, find (a) how long it will take the body to reach a temperature of
75° F and (b) the temperature of the body after 20 minutes.


# Differantial form of the problem ; dT/dt + k*T = 100 * k
# We will look only numerical solutions.
# The general solution of the problem ; T = -50 * exp(-k*t) + 1

import numpy as np
import pandas as pd
from scipy.integrate import odeint
import matplotlib.pyplot as plt

def temperature(T,t):
    k = 0.045
    dTdt = k * (100 - T)
    return dTdt
T0 = 50


t = np.linspace(0,25)

T = odeint(temperature,T0,t)
plt.title("Problem 7.9 ")
plt.plot(t,y)
plt.xlabel('Time')
plt.ylabel('T(t)')
plt.show()

K = np.squeeze(np.asarray(T))
M = np.squeeze(np.asarray(t))
print(K)
print(M)

